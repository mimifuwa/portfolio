---
import MainLayout from "../layouts/mainLayout.astro";
const posts = await Astro.glob("./posts/*.mdx");
---

<MainLayout title="Blog" category="blog">
    
    <section>
    <h2>記事一覧 <span class="font-thin sub-text"> - Articles</span></h2>
    <p>今移行中です....</p>
        <!-- <ul>
            {
                posts.map(post => {
                    return <li><a href={post.url}>{post.frontmatter.title}</a></li>
                })
            }
        </ul> -->
    </section>

</MainLayout>